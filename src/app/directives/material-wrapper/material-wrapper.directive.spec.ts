import { MaterialWrapperDirective } from './material-wrapper.directive';

describe('MaterialWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new MaterialWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
